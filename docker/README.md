# Docker

Arquivos auxiliares de infraestrutura local.

O arquivo principal de orquestracao fica na raiz do projeto:

```bash
docker compose up --build
```

Nesta pasta ficam configuracoes complementares, como o OpenTelemetry Collector e o import do realm do Keycloak.
